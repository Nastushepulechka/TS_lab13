import { FilterForFirstnamePipe } from './filter-for-firstname.pipe';

describe('FilterForFirstnamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterForFirstnamePipe();
    expect(pipe).toBeTruthy();
  });
});
