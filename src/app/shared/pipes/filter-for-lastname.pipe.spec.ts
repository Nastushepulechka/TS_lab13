import { FilterForLastnamePipe } from './filter-for-lastname.pipe';

describe('FilterForLastnamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterForLastnamePipe();
    expect(pipe).toBeTruthy();
  });
});
